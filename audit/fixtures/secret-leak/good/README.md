<!-- TEST FIXTURE (secret-leak / good) -->
# Example app

Secrets are provided via environment variables only. Copy `.env.example` to
`.env` (gitignored) and fill in values locally. Nothing secret is committed.
