# handoff_strayed

THE NEGATIVE CONTROL. A handoff document dumped at the tree ROOT instead of its
canonical data/handoffs/ dir. cleanup MUST flag this as stray (and, under --apply,
move it to data/handoffs/).
