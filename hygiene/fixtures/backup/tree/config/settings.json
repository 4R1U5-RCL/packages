{
  "_comment": "Fixture config file (obviously-fake placeholder values only) so the backup tree has more than one entry across more than one directory.",
  "theme": "placeholder",
  "model": "placeholder-model"
}
